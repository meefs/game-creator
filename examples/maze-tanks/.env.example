# Override the deployed SERVER_URL (in src/core/Constants.js) for local dev.
# Use http://127.0.0.1:1999 to point at `npx partykit dev` running locally.
# Leave unset (or comment out) to use the deployed Cloudflare URL.
VITE_MULTIPLAYER_SERVER_URL=http://127.0.0.1:1999
